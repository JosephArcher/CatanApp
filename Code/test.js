function imageMapper (tileType) {
	
	if(tileType == "Wood") {
		// Wood Image
	}

}


function( d , i ) {

 // Get the next cantan tile from the array
 var nextCatanTile = this.catanTilesArray[i];

 //Get the image for the tile
 var image this.imageMapper(nextCatanTile.tileType);




}